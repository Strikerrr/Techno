//const { Command } = require('discord-akairo');

//Create the command class below, replace NameCommand with the name of the command (eg BanCommand)

/*
class NameCommand extends Command {
    constructor() {
        super('name', {
                aliases: ['Add any aliases for the command in here with '' '],
                args: [{
                    id: 'name of arg',
                    type: 'string/integer',
                    match: 'LEAVE THIS AS 'content' '
                }]
            }),

            this.help = {
                'help': {
                    name: 'name',
                    description: 'Brief description',
                    category: 'Whatever folder this fits into',
                    usage: 't!name <any args>',
                    aliases: 'Any aliases',
                    DMs: 'Yes/No',
                    UserPerms: 'Permissions needed for the user to do this',
                    TechnoPerms: 'Permissions Techno needs to do this, mostly Send Messages'
                }
            };
    }

    exec(msg, args) {
     command code goes here
    }
}

module.exports = NameCommand; //Exporting the classs
*/